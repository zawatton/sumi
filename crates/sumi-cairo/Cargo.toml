# Native Cairo/GTK4 backend for sumi.
#
# Needs the GTK4 dev libraries (cairo, pango, gtk4) and a GNU Rust toolchain on
# Windows (MSYS2 GTK4 is MinGW, not MSVC). See ../../README.org "Setup".
# Build/run with:
#   PKG_CONFIG_PATH=/mingw64/lib/pkgconfig PATH=/mingw64/bin:$PATH \
#     cargo +stable-x86_64-pc-windows-gnu run -p sumi-cairo --bin <name>

[package]
name = "sumi-cairo"
version = "0.0.0"
edition = "2021"
description = "Native Cairo/GTK4 backend for sumi (real surfaces, Pango text, GTK4 window)"
license = "CC0-1.0"

[dependencies]
sumi-core = { path = "../sumi-core" }
cairo-rs = { version = "0.20", features = ["png"] }
pango = "0.20"
pangocairo = "0.20"
gtk4 = "0.9"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

# headless PNG render (no window) — proves Cairo + Pango
[[bin]]
name = "sumi-cairo-demo"
path = "src/main.rs"

# live GTK4 window — proves gtk4-rs links and renders natively
[[bin]]
name = "sumi-window"
path = "src/window.rs"

# render a captured real game frame (frame.json) natively in a GTK4 window
[[bin]]
name = "sumi-frame"
path = "src/frame.rs"
