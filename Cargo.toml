[workspace]
resolver = "2"
members = [
    "crates/sumi-core",
    "crates/sumi-skia",
    "crates/sumi-cairo",
    "crates/sumi-input",
]
# shells/ (Tauri etc.) are standalone crates with their own workspace — kept out
# of `cargo test --workspace` / CI so the heavy webview dep tree isn't built there.
exclude = ["shells"]
